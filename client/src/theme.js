import { createTheme } from '@material-ui/core/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: '#fffdfa',
    },
    secondary: {
      main: '#FF8E53',
    },
  },
  typography: {

  }
});

export default theme;